{
  "name": "fext-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "dev": "dotnet fable src/Website.fsproj -e .jsx --watch -o ./ --run next dev",
    "build": "dotnet fable src/Website.fsproj -e .jsx && next build"
  },
  "dependencies": {
    "esbuild": "^0.16.4",
    "gray-matter": "^4.0.3",
    "mdx-bundler": "^9.0.1",
    "next": "^13.0.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
